name: xyz_service_brokers
environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
  xyz_utils:
    git:
      url: https://github.com/robmllze/xyz_utils.git
      ref: master
  xyz_pod:
    git:
      url: https://github.com/robmllze/xyz_pod.git
      ref: master
  meta:
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter: